package atividade_3.q_7;

public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        System.out.print("Digite o número de cartelas: ");
        int numCartelas = scanner.nextInt();
        List<Cartela> cartelas = new ArrayList<>();
        for (int i = 0; i < numCartelas; i++) {
            cartelas.add(new Cartela());
        }
        Bingo bingo = new Bingo();
        for (Cartela cartela : cartelas) {
            bingo.registerObserver(cartela);
        }
        bingo.jogar();
    }
}
