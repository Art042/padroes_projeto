package atividade_3.q_5;

class LocaleFactory {
    public static AbstractFactory getFactory(String localeCode) {
        switch(localeCode) {
            case "BR":
                return new BrazilFactory();
            case "US":
                return new USAFactory();
            default:
                throw new IllegalArgumentException("Código de localidade inválido.");
        }
    }
}